# Data_warehouse_project
A modern data warehouse built with SQL Server using the Medallion Architecture (Bronze, Silver, Gold) — covering ETL pipelines, data modeling, and analytics from raw CRM/ERP data to a business-ready star schema.
